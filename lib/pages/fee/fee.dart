import 'package:flutter/material.dart';

class Fee extends StatelessWidget {
  const Fee({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tarifa de servicios"),
    );
  }
}
